A labelled text field — mono label over a soft cream surface with a hairline border.

```jsx
<Input label="Email" placeholder="you@studio.com" value={v} onChange={e => setV(e.target.value)} />
```

Forwards all native `<input>` props (`type`, `name`, etc).
